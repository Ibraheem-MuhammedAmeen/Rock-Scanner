import 'package:flutter/material.dart';

class IgneousRock extends StatelessWidget {
  const IgneousRock({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Igneous'),
    );
  }
}
