import 'package:flutter/material.dart';

class MineralRock extends StatelessWidget {
  const MineralRock({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Mineral'),
    );
  }
}
